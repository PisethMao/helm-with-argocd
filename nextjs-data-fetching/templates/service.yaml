apiVersion: v1
kind: Service
metadata:
  name: {{ include "nextjs-data-fetching.fullname" . }}
  labels:
    {{- include "nextjs-data-fetching.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nextjs-data-fetching.selectorLabels" . | nindent 4 }}
