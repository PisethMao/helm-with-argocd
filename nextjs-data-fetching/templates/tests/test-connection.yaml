apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nextjs-data-fetching.fullname" . }}-test-connection"
  labels:
    {{- include "nextjs-data-fetching.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nextjs-data-fetching.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
